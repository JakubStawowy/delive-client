
import {ModalTemplate} from "../templates/ModalTemplate";

export const Test = (props) => {

    return (<div>
        xd
        <ModalTemplate
            child={<div>testivo</div>}
        />
    </div>)
}
